{
	"name": "collaborate-x",
	"private": true,
	"workspaces": [
		"server"
	],
	"scripts": {
		"build": "npm --workspace server run build",
		"start": "npm --workspace server start",
		"server:build": "npm --workspace server run build",
		"server:start": "npm --workspace server start",
		"client:build": "npm --prefix client run build"
	}
}
